<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LNMTrack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CircleImage">
        <item name="cornerSize">50%</item>
    </style>

    <style name="ProfileInputContainerLocked">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">52dp</item>
        <item name="android:layout_marginBottom">12dp</item>
        <item name="android:background">@drawable/bg_input_locked</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:enabled">false</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingHorizontal" tools:ignore="NewApi">15dp</item>
    </style>

    <style name="ProfileInputContainerEditable" parent="ProfileInputContainerLocked">
        <item name="android:background">@drawable/bg_input_editable</item>
        <item name="android:textColor">@color/black</item>
        <item name="hintTextColor">@color/black</item>
    </style>

    <style name="ProfileInputIcon">
        <item name="android:layout_width">20dp</item>
        <item name="android:layout_height">20dp</item>
        <item name="android:tint">#888888</item>
    </style>

    <style name="ProfileEditTextLocked">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginStart">10dp</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:enabled">false</item>
        <item name="android:textColor">#121212</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="Theme.LNMTrack" parent="Base.Theme.LNMTrack" />
</resources>